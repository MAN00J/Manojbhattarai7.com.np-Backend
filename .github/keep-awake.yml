name: Keep Render Awake


on:
  schedule:
    
    - cron: '*/10 * * * *'
  workflow_dispatch: 

jobs:
  ping-job:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Render
        
        run: curl -s https://my-backend-v0sn.onrender.com > /dev/null